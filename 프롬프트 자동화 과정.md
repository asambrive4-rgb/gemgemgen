# 프롬프트 자동화 과정

이 문서는 사용자가 **시작** 버튼을 누른 뒤 자동화가 끝날 때까지의 흐름을 UX 중심으로 간단히 정리한 문서입니다.

## 전체 흐름 요약

- 사용자는 자동화 화면에서 원본 프롬프트와 반복 횟수를 준비한 뒤 **시작** 버튼을 누릅니다.
- 앱은 실행 가능 상태를 확인하고, 플로팅 바를 띄운 뒤 Gemini 앱으로 이동합니다.
- 먼저 세션 시작 마커를 전송하고, 이후 반복 횟수만큼 프롬프트를 하나씩 새 채팅에 전송합니다.
- 각 단계에서 Gemini 버튼이나 입력창을 못 찾으면 잠깐 기다렸다가 다시 시도합니다.
- 완료, 실패, 사용자의 중지 중 하나로 끝나며, 마지막에는 원래 입력기로 복구하고 실행 로그를 남깁니다.

## 전체 과정 흐름

| 순서 | 단계 | 사용자에게 보이는 것 | 내부에서 일어나는 일 | 기본 대기 | 실패 시 재시도 대기 |
| --- | --- | --- | --- | --- | --- |
| 1 | 시작 버튼 클릭 | **시작**이 **중지**로 바뀜 | 중복 실행 여부와 입력 가능 상태 확인 | 거의 즉시 | 재시도 없음 |
| 2 | 플로팅 바 준비 | 플로팅 바가 뜨고 앱이 뒤로 이동 | 다른 앱 위 표시 권한 확인, 실행 상태 연결 | 거의 즉시 | 권한이 없으면 설정 화면으로 이동 |
| 3 | 실행 준비 | `자동화 준비 중` 상태 표시 | 마지막 실행값 저장, 원본 프롬프트 클립보드 보관, 와일드카드 파일 1회 로드 | 파일 수와 기기 성능에 따라 달라짐 | 파일 로드 실패 시 재시도 없이 실패 종료 |
| 4 | 사전 검증 | 문제가 있으면 실패 메시지 표시 | 접근성 서비스, 원본 프롬프트, Null Keyboard 전환 가능 여부 확인 | 보통 짧음 | 조건이 안 맞으면 재시도 없이 실패 종료 |
| 5 | Gemini 실행 | Gemini 앱으로 화면 전환 | Gemini 앱 실행 요청 | Gemini 앱 실행 속도에 따라 달라짐 | 앱을 못 찾으면 재시도 없이 실패 종료 |
| 6 | 세션 마커용 새 채팅 열기 | `사이드바 여는 중`, `새 채팅 찾는 중` 등 표시 | 사이드바와 새 채팅 버튼을 찾아 클릭 | 버튼이 바로 보이면 거의 즉시 | 처음 3초는 0.25초 간격, 이후 1초 간격, 총 10초까지 재시도 |
| 7 | 세션 마커 입력 | `입력창 찾는 중`, `프롬프트 입력 반영 확인 중` 표시 | 입력창에 시작 마커 입력 | 입력 후 0.5초 대기 | 입력창을 못 찾거나 입력 반영 실패 시 총 10초까지 재시도 |
| 8 | 세션 마커 전송 | `보내기 버튼 활성화 대기 중`, `전송 확인 중` 표시 | 보내기 버튼 클릭 후 입력창이 비었는지 확인 | 클릭 후 0.5초 대기 | 보내기 버튼을 못 찾거나 전송 확인 실패 시 총 10초까지 재시도 |
| 9 | 반복 프롬프트 생성 | `[현재/전체]` 진행 표시 | 현재 회차의 최종 프롬프트 1개 생성 | 거의 즉시 | 생성 자체는 별도 재시도 없음 |
| 10 | 반복 프롬프트용 새 채팅 열기 | `새 채팅 찾는 중` 표시 | 이번 회차를 위한 새 채팅 클릭 | 버튼이 바로 보이면 거의 즉시 | 처음 3초는 0.25초 간격, 이후 1초 간격, 총 10초까지 재시도 |
| 11 | 반복 프롬프트 입력 | `입력창 찾는 중`, `프롬프트 입력 반영 확인 중` 표시 | 입력창에 생성된 프롬프트 입력 | 입력 후 0.5초 대기 | 입력창 탐색/입력 확인 실패 시 총 10초까지 재시도 |
| 12 | 반복 프롬프트 전송 | `보내기 버튼 활성화 대기 중`, `전송 확인 중` 표시 | 보내기 클릭 후 전송 완료 확인 | 클릭 후 0.5초 대기 | 보내기 버튼 탐색/전송 확인 실패 시 총 10초까지 재시도 |
| 13 | 다음 회차 이동 | `[현재/전체]` 숫자가 다음 회차로 바뀜 | 성공 카운트 증가 후 다음 프롬프트 생성 | 거의 즉시 | 마지막 회차까지 9-12번 반복 |
| 14 | 종료 처리 | 성공, 실패, 중지 중 하나 표시 | 원래 입력기로 복구하고 실행 로그 저장 | 입력기 복구 시간만큼 대기 | 복구 실패 시 실패 상태로 표시 |

z

| 흐름 | 성공하면 | 실패하면 |
| --- | --- | --- |
| 1. 새 채팅 찾기 | 바로 클릭하고 다음 단계로 이동 | 3초까지 0.25초 간격 재시도, 이후 1초 간격 재시도 |
| 2. 입력창 찾기 | 입력창에 포커스 | 3초까지 0.25초 간격 재시도, 이후 1초 간격 재시도 |
| 3. 프롬프트 입력 | 텍스트 직접 입력 후 0.5초 대기 | 입력 실패 시 같은 재시도 규칙 적용 |
| 4. 입력 반영 확인 | 입력창에 프롬프트가 있으면 다음 단계로 이동 | 반영되지 않으면 같은 재시도 규칙 적용 |
| 5. 보내기 버튼 대기 | 버튼이 활성화되면 클릭 | 버튼이 없거나 비활성화면 같은 재시도 규칙 적용 |
| 6. 전송 확인 | 클릭 후 0.5초 대기, 입력창이 비면 성공 처리 | 입력창에 프롬프트가 남아 있거나 확인 불가면 같은 재시도 규칙 적용 |
| 7. 회차 종료 | 성공 카운트를 올리고 다음 회차로 이동 | 한 탐색/확인 단계가 총 10초를 넘기면 실패 종료 |

## 대기와 재시도 기준

| 상황 | 대기 방식 |
| --- | --- |
| Gemini UI 요소를 못 찾음 | 해당 단계 시작 후 처음 3초 동안 0.25초 간격으로 빠르게 재시도 |
| 3초 이후에도 못 찾음 | 같은 단계에서 1초 간격으로 재시도 |
| 같은 탐색/확인 단계가 10초 이상 실패 | 자동화 실패로 종료 |
| 프롬프트 입력 후 확인 | 0.5초 대기 후 입력 반영 여부 확인 |
| 보내기 클릭 후 확인 | 0.5초 대기 후 전송 완료 여부 확인 |

## 시간 흐름 예시

아래는 한 번의 프롬프트 전송이 막힘 없이 진행될 때의 체감 흐름입니다. Gemini 앱 반응이 느리면 새 채팅 찾기, 입력창 찾기, 보내기 대기 단계에서 시간이 더 늘어납니다.

```text
시작
→ 새 채팅 찾기/클릭
  - 못 찾으면 0.25초 간격 재시도
  - 3초 이후에는 1초 간격 재시도
  - 10초를 넘기면 실패
→ 입력창 찾기
  - 못 찾으면 같은 재시도 규칙 적용
→ 프롬프트 입력
→ 0.5초 대기 후 입력 확인
  - 입력이 반영되지 않으면 같은 재시도 규칙 적용
→ 보내기 버튼 찾기/클릭
  - 버튼이 없거나 비활성화면 같은 재시도 규칙 적용
→ 0.5초 대기 후 전송 확인
  - 전송 확인이 안 되면 같은 재시도 규칙 적용
→ 다음 회차 또는 종료
```

## 사용자가 체감하는 UX

- 자동화가 시작되면 앱 화면보다 Gemini 화면과 플로팅 바를 주로 보게 됩니다.
- 상태 문구는 현재 앱이 무엇을 찾고 있는지 보여줍니다. 예: 새 채팅 찾는 중, 입력창 찾는 중, 보내기 버튼 활성화 대기 중.
- 반복 전송 중에는 현재 몇 번째 프롬프트인지 `[현재/전체]` 형태로 확인할 수 있습니다.
- 실행 중에도 플로팅 바의 `＋`/`−` 로 목표 개수를 바로 바꿀 수 있습니다. 늘리면 그만큼 더 보내고, 이미 보낸 개수보다 줄일 수는 없습니다. 바꾼 값은 다음 실행 기본값으로 저장됩니다.
- Gemini 화면이 늦게 뜨거나 버튼이 늦게 활성화되면 자동화가 잠시 기다리며 재시도합니다.
- 10초 안에 필요한 버튼이나 입력창을 찾지 못하면 멈추고 실패 이유를 보여줍니다.

## 종료 상태

| 종료 상태 | 의미 |
| --- | --- |
| 자동화 성공 | 세션 마커와 모든 반복 프롬프트 전송이 끝남 |
| 자동화 실패 | 권한, 입력기, Gemini UI 탐색, 전송 확인 중 문제가 발생함 |
| 자동화 중지됨 | 사용자가 실행 중 **중지** 버튼을 눌러 멈춤 |

종료 후에는 가능한 한 원래 입력기로 복구하고, 최근 실행 로그에 결과를 남깁니다.
